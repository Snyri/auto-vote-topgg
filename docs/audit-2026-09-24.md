# Repository review — 2026-09-24

Reviewed the complete tracked repository at `8f80adaac645a7a483ba14d2771940d4bd296c47`: voting/authentication/browser lifecycle, reporting, scheduler/container, both workflows, dependency audit and pins, all tests, configuration, ownership, documentation, task history, and both architecture assets. GitHub retained 66 workflow runs: 21 vote runs and 45 security runs. All 21 available vote-job logs were read. Deleted historical runs cannot be reconstructed from this review.

## Cloudflare evidence

| Run | Observation | Interpretation |
| --- | --- | --- |
| [139: primary](https://github.com/Snyri/auto-vote-topgg/actions/runs/35982936591) | Four session probes returned HTTP 403 HTML and `cf-mitigated: challenge`; no Vote click | Confirmed Cloudflare Challenge Pages, without proof of expired credentials |
| [140: recovery](https://github.com/Snyri/auto-vote-topgg/actions/runs/35983141203) | Same sequence on another runner after the widget disappeared | Disappearance was prematurely described as clearance; replacing the runner is not a guaranteed cure |
| [136: recovery](https://github.com/Snyri/auto-vote-topgg/actions/runs/35835165750) | Vote followed by persisted confirmation after reload | Stronger evidence than a click or same-page success text |
| [137](https://github.com/Snyri/auto-vote-topgg/actions/runs/35913826369), [138](https://github.com/Snyri/auto-vote-topgg/actions/runs/35974310817) | Click initially uncertain; later attempt observed cooldown | Green confirms cooldown, without proving which attempt created it |
| [132](https://github.com/Snyri/auto-vote-topgg/actions/runs/35776734528), [133](https://github.com/Snyri/auto-vote-topgg/actions/runs/35777645452) | Both claimed success roughly eight minutes apart for the same configured account/bot | Older same-DOM success claims are unreliable |

Ten retained vote runs were green and eleven red. Only one green run explicitly logged persisted confirmation; two reached cooldown after uncertain clicks; seven used the earlier, weaker success check. These are log observations, not an independently verified vote ledger.

[Cloudflare's response documentation](https://developers.cloudflare.com/cloudflare-challenges/challenge-types/challenge-pages/detect-response/) identifies `cf-mitigated: challenge` as the specific Challenge Page marker. A `server: cloudflare` header alone does not identify the blocking rule. [Challenge Pages interrupt fetch/XHR](https://developers.cloudflare.com/cloudflare-challenges/challenge-types/challenge-pages/), and [automated browsers are unsupported for production challenges](https://developers.cloudflare.com/cloudflare-challenges/reference/supported-browsers/). The logs do not reveal the site's WAF rule or establish that runner IP reputation caused it.

## Changes

- **Authentication readiness:** recognize managed interstitial titles/containers and require recognizable top.gg UI after a detected challenge before probing the session. Waits only observe the page, without extra navigation. Widget disappearance is no longer logged as verified access. Public Vote navigation links cannot authenticate a session.
- **Bounded session request:** a browser AbortController limits fetch/body processing to 12 seconds; an outer asynchronous timeout also covers a stalled browser connection. Malformed probe results cannot authenticate an account.
- **Partial results:** a later bot exception preserves earlier completed votes. Account-wide authentication/protection failures on a retry apply only to pending bots. Missing results become explicit failures. Duplicate bot IDs are collapsed, and IDs must use ASCII digits.
- **Cleanup:** unexpected teardown errors cannot overwrite completed voting results. A profile is still deleted after forced termination succeeds. Error screenshots are removed even without Telegram configured.
- **Scheduler:** preserve accepted run IDs and deadlines across dispatch cycles, query a known run directly when lists regress, and respect the later of a failed-run defer deadline and a prior successful future deadline. Refreshes are throttled. Artifact downloads have a streaming size cap and require exactly the expected ZIP entry.
- **Workflow:** serialize voting across branches, propagate artifact/infrastructure errors independently of the Python exit status, validate recovery depth strictly, and read bounded recovery ZIPs without extracting paths.
- **Dependency audit:** follow bounded OSV pagination; reject malformed, incomplete, or unsupported audit inputs. All 12 pinned packages passed a live OSV query. Dependency and Action versions were retained.
- **Documentation:** replace obsolete architecture images with a maintained Mermaid flow, correct multiline local configuration examples, and remove inaccurate assertions that branch protection is enforced. GitHub reported `master` unprotected and no repository rulesets.

## Verification and operating limits

Local Windows/Python 3.12 verification passed the full test suite, including actual embedded JavaScript executed against controlled DOM/fetch fixtures in Node. New partial-result/timeout regressions were checked against the original code, where they failed. Browser interactions, votes, and Telegram delivery are simulated.

The local environment uses the same pinned package versions with Windows wheels. The hash lock targets Linux/Python 3.11; GitHub's `test` job checks that installation. `dependency-audit` and `scheduler-image` provide separate advisory and container checks. Consult the pull request for authoritative CI results and final test count.

There was no live vote submission as part of this review. Passing tests cannot establish that Cloudflare will accept a future automated request. Persistent denial remains an external condition requiring site-operator resolution or ordinary interactive access; the existing bounded recovery policy is retained without a promise of clearance.

Scheduler changes require the Northflank service to rebuild and deploy the updated image. GitHub merging or container CI alone does not verify that deployment. The single timestamp artifact also does not identify configuration changes: adding an account/bot while a prior successful deadline is pending can require an intentional manual run.

## Follow-up: persistent challenge classified as manual CAPTCHA

[Run 141](https://github.com/Snyri/auto-vote-topgg/actions/runs/36054709500) stopped during cookie authentication after protection remained detected for 30 seconds. No session probe or Vote click occurred. Unlike runs 139/140, it reported `captcha_required`, skipped the protection recovery marker and scheduled a one-hour delay. The logs do not preserve the exact page DOM, so they do not establish that a manual CAPTCHA was actually required. Telegram was not configured, so the screenshot was not delivered.

The expanded challenge detector introduced a classification regression: a Cloudflare interstitial could time out and take the same terminal path as a standalone CAPTCHA. The follow-up distinguishes a full-page challenge still present at the timeout from an embedded widget. Persistent interstitials use `blocked` and the existing bounded recovery policy; standalone CAPTCHA behavior is preserved. The distinction applies to cookie/OAuth authentication and all five challenge checks around voting. A post-click block explicitly reports the vote outcome as unverified.

Regression tests cover the previous failure, real embedded JavaScript against controlled page fixtures, authentication/OAuth propagation, all five voting phases, and unchanged recovery limits. This corrects result classification; it does not demonstrate that Cloudflare will accept the next request.

## Follow-up: restore authentication paths restricted by the audit

[Runs 142](https://github.com/Snyri/auto-vote-topgg/actions/runs/36056842024) and [143](https://github.com/Snyri/auto-vote-topgg/actions/runs/36057245802) each exhausted four 30-second challenge waits across two browsers, without reaching a session probe or clicking Vote. The one fresh-workflow recovery operated as intended. In contrast, the last successful [run 138](https://github.com/Snyri/auto-vote-topgg/actions/runs/35974310817) progressed past widget disappearance, sometimes received a 403 session response, and eventually reached the vote page. Chrome was the same version in these runs. The user also confirmed that the vote page opens normally in their usual browser; this does not isolate runner IP reputation as the cause.

The audit had introduced overly restrictive authentication paths: a residual interstitial title or container could keep the widget loop active and hide a usable Vote control; an unknown DOM state after verification could no longer fall back to the session endpoint. Regression fixtures reproduce these failures in the audit code, including a valid session being rejected without checking it. They do not establish the exact DOM of the failed runs.

This follow-up selectively restores the pre-audit widget detection and application-control precedence, and restores the session-probe fallback after bounded DOM settling. HTTPS/origin/loading checks, request timeouts, strict session-response validation, partial-result preservation, cleanup and scheduler fixes remain. A failed session probe still reports blocked; a dispatched click still does not count as verified access or a successful vote.

Challenge diagnostics now record only fixed boolean signals and a loading-state enum at detection, click failure and timeout. They distinguish present versus visible interstitial containers, visible application controls and token presence without logging page text, URLs, account data, or token values. Collection has a two-second limit and cannot replace the original result on failure. The verification-click log now explicitly says that neither target matching nor acceptance is confirmed by the library call.

The restored paths are checked with JavaScript fixtures and session-response tests; diagnostics additionally have privacy, malformed-response and cancellation tests. A successful live workflow remains necessary to confirm recovery in the real environment.

## Follow-up: acknowledgement lost during post-vote verification

The user confirmed that the vote was received, while [run 144](https://github.com/Snyri/auto-vote-topgg/actions/runs/36059664563) and its [recovery run 145](https://github.com/Snyri/auto-vote-topgg/actions/runs/36059991873) both failed. The logs show Vote clicks at 21:10:21 and 21:13:58 UTC. The mandatory verification reload then encountered a Cloudflare challenge. Subsequent browser attempts failed session validation with `403` and `cf-mitigated: challenge`; they replaced the original unconfirmed submission in the final report. Artifact uploads, recovery and cleanup jobs did not cause these failures. Slow Chrome startup was recovered successfully. The logs do not show which submission produced the received vote, nor do they preserve the immediate post-click page.

The browser now observes the current page before and after the click. A new strong success/cooldown signal must persist across two checks, on the exact HTTPS bot vote page, with no enabled Vote control, visible challenge, login requirement or explicit error. This is application acknowledgement, not a fabricated API receipt or proof from a generic HTTP 200. It avoids the mandatory reload after acknowledged success while retaining the independent reload check when acknowledgement is absent or ambiguous. Pre-existing success phrases cannot pass the new path.

Every unconfirmed post-click outcome, including a browser/command failure, now carries an internal submission flag. That bot is excluded from immediate retry; later failures for other bots cannot overwrite its result. Automatic fresh-workflow protection recovery is suppressed while any such submission remains unconfirmed, because the recovery workflow otherwise processes every account/bot again. Other pending bots can still retry within the same run. Scheduled and manual future runs have no persistent submission ledger; this change does not claim cross-run exactly-once voting.

Tests reproduce acknowledgement followed by a would-be failed reload, delayed acknowledgement after verification, ambiguous fallback, post-click disconnection and mixed-bot retries. JavaScript fixtures check old/optimistic text, active controls, visible versus hidden challenge containers, wrong origins/pages, malformed data, stable observations and bounded observation time. Genuine failures still produce a failing Action; no exit status is forced to success. The next live run is needed to validate the site's current acknowledgement against this new observer.

## Follow-up: run 146 and a no-submission inspection

The user clarified that the received vote was around 23:10–23:14 CEST (21:10–21:14 UTC), corresponding to runs 144/145, not the later [run 146](https://github.com/Snyri/auto-vote-topgg/actions/runs/36062264243). Run 146 first received a `403` session response, then inferred authentication from the vote-page controls at 21:34:46 UTC and logged a Vote click at 21:34:49 UTC. No fresh page acknowledgement was confirmed. The fallback reload at 21:35:00 UTC led to an unauthenticated state, and the submission remained unconfirmed. These logs do not establish a successful vote in run 146 or identify which DOM element received the click.

Code review and JavaScript fixtures reproduced a pre-existing control-selection defect: the first visible element labelled Vote could be a public navigation link rather than a submission button; an earlier disabled control could also hide a later enabled one. Selection now excludes ordinary navigation anchors, supports native buttons and non-navigating controls with `role="button"`, and prefers an enabled candidate. Old selection markers are cleared before choosing a target. Logs expose only the selected control category and a bounded candidate count. The tests demonstrate the defect and correction; the preserved live evidence does not prove that it caused run 146.

The separate [inspection run](https://github.com/Snyri/auto-vote-topgg/actions/runs/36063721624) opened the first configured bot's vote page with one cookie session and made no vote submission, OAuth request or CAPTCHA-solving attempt. It reported three cookies and no explicit expiry timestamp in the past. At 21:48:21, 21:48:33 and 21:48:55 UTC, every observation showed the exact bot vote path, a complete document, a challenge title, an active challenge, no relevant visible Vote/Login controls, and an unknown authentication hint. The final session check returned HTTP `403` with `cf-mitigated: challenge`; the request correlation ID was `a404ff004d3c6de9-DFW`.

This inspection confirms a Cloudflare access block during that particular run. It does not prove cookie revocation, establish the cause of run 146, validate the repaired selector against the live voting page, or verify a new vote. Cookie expiry metadata alone cannot prove session validity. The diagnostic completed successfully because its purpose was to observe the page, not to vote or clear protection.

The inspection workflow remains available through manual dispatch. Its temporary initial push trigger is removed after collecting this evidence. It shares the voting concurrency group and leaves vote scheduling artifacts untouched. Output is restricted to fixed categories, booleans and bounded numeric metadata; no raw page content, cookie values, screenshots or account details are published. The control-selection correction still requires validation in a normal vote run that reaches the application page.
